import { MochiCollectionScreen } from "@/features/collection/components/MochiCollectionScreen";

/** 📖 도감 — 모찌 뽑기 수집 = 리텐션 엔진 (PRD 12). */
export default function CollectionPage() {
  return <MochiCollectionScreen />;
}
