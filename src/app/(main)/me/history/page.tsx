import { MealHistoryScreen } from "@/features/record/components/MealHistoryScreen";

/** ⏳ 기록 되돌아보기 (마이 전용 화면, PRD 6장 회고). 숫자는 마이 트리에서만 (불변 #2). */
export default function HistoryPage() {
  return <MealHistoryScreen />;
}
