import { WeightScreen } from "@/features/record/components/WeightScreen";

/** ⚖️ 체중 기록 (마이 전용 화면, PRD 6장). 숫자는 마이 트리에서만 (불변 #2). */
export default function WeightPage() {
  return <WeightScreen />;
}
