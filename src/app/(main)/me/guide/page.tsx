import { FeatureGuideScreen } from "@/features/mochi/components/FeatureGuideScreen";

/** 📖 모찌 사용법 (마이 > 모찌 사용법) — 페이지는 렌더만. */
export default function GuidePage() {
  return <FeatureGuideScreen />;
}
