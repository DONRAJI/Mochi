# 서버 전용 (NEXT_PUBLIC_ 접두사 금지 — 붙이면 브라우저에 평문 노출)
DATABASE_URL=
SESSION_SECRET=
# 외부 API 키 (서버 Route Handler에서만 사용)
NUTRITION_API_KEY=
VISION_API_KEY=

# 클라 공개용만 NEXT_PUBLIC_ 사용 (절대 시크릿 X)
NEXT_PUBLIC_APP_URL=http://localhost:3000
